'use client';import {FormEvent,useState} from 'react';import {supabase} from '@/lib/supabase/client';import {useRouter} from 'next/navigation';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),router=useRouter();async function login(e:FormEvent){e.preventDefault();const {error}=await supabase().auth.signInWithPassword({email,password});if(error)return setError(error.message);router.push('/admin')}return <main className="flex min-h-screen items-center justify-center p-5"><form onSubmit={login} className="glass w-full max-w-md p-8"><p className="font-display tracking-[.18em] text-gold">REIMEI MUN</p><h1 className="mt-5 font-display text-3xl">Admin access</h1><label className="label mt-8 block">Email<input className="field" type="email" onChange={e=>setEmail(e.target.value)} required/></label><label className="label mt-5 block">Password<input className="field" type="password" onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="mt-4 text-sm text-red-300">{error}</p>}<button className="btn-primary mt-7 w-full">Sign in</button></form></main>}
